[package]
name = "para-core"
description = "UI-agnostic core for Para Bellum: buffer, cursor, undo, command bus"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
ropey = "1"

[dev-dependencies]
